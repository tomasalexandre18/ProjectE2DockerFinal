services:
  fastapi-stripe:
    restart: always
    working_dir: /usr/src/app
    expose:
      - "5085"
    volumes:
      - /usr/src/app
    build: .
    image: sgttabasco/ecommerce-fastapi-stripe:part-2
